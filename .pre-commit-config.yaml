# Pre-commit configuration
default_install_hook_types: [pre-commit, pre-push, commit-msg]
default_stages: [pre-commit]

repos:
  # Ruff for linting and formatting
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.4
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  # MyPy for type checking
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.17.0
    hooks:
      - id: mypy
        name: MyPy (Type checking)
        additional_dependencies: [
          types-requests, 
          types-PyYAML,
          pydantic>=2.11.0,
          packaging>=23.0,
          fastapi>=0.115.0,
          httpx>=0.24.0,
          pytest>=8.4.0,
          pytest-asyncio>=1.1.0
        ]
        files: ^src/
        args: [--config-file, pyproject.toml, --explicit-package-bases]

  # Vulture for dead code detection
  - repo: https://github.com/jendrikseipp/vulture
    rev: v2.14
    hooks:
      - id: vulture
        name: Vulture (Dead code detection)
        args: [src/, tests/]

  # Code coverage check
  - repo: local
    hooks:
      - id: coverage-check
        name: Code Coverage (90%+ required)
        entry: python scripts/check_coverage.py
        language: system
        stages: [pre-commit]
        description: "Ensure minimum 90% code coverage"
        always_run: true
        pass_filenames: false
        verbose: true

  # Pydantic v1 syntax detection
  - repo: local
    hooks:
      - id: pydantic-v1-check
        name: Pydantic v1 Syntax Check
        entry: python scripts/check_pydantic_v1.py
        language: system
        stages: [pre-commit]
        description: "Check for deprecated Pydantic v1 syntax patterns"
        always_run: true
        types: [python]

  # Release guardrails check
  - repo: local
    hooks:
      - id: release-guardrails
        name: Release Guardrails Check
        entry: python scripts/check_release_guardrails.py
        language: system
        stages: [pre-commit]
        description: "Check for proper version bumps and changelog updates on release commits"
        always_run: true 